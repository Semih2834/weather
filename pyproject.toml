[tool.poetry]
name = "project"
version = "0.1.0"
description = "NJ Weather Data using NOAA Rest API"
authors = ["Your Name <you@example.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
